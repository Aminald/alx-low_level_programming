#include "main.h"
/**
check_if_digit - checks if a given character is a digit (0-9)
@c: the character to be checked
Return: 1 if c is a digit, 0 otherwise
*/
int _isdigit(int c)
{
return (c >= '0' && c <= '9');
}
