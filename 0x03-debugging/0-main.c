#include "main.h"
/**
 * main - tests whether the function positive_or_negative() correctly
 *        identifies if a number is positive, negative, or zero
 *
 * Return: Always 0
 */
int main(void)
{
int i;
i = 0;
positive_or_negative(i);
return (0);
}
